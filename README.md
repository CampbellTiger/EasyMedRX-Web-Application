# EasyMedRX-Web-Application

To launch the code, make sure you have all the requirements in requirements.txt installed.

Then in WSL, use command
python startup.py

This will open up the virtual environment, run the web application and all background processes. 
